{
  "name": "sih26171-extension",
  "version": "0.1.0",
  "private": true,
  "description": "M0 foundation — Chrome Manifest V3 extension for SIH26171",
  "type": "module",
  "scripts": {
    "build": "tsc && node scripts/build.mjs",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'lint: no linter configured yet'",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.9",
    "esbuild": "^0.21.5",
    "happy-dom": "^20.14.0",
    "typescript": "^5.4.0",
    "vitest": "^1.4.0"
  }
}
